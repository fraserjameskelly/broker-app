container_commands:
    01_install_serve:
        env:
            NODE_ENV: production
        command: "npm install -g serve"
    02_serve:
        env:
            NODE_ENV: production
        command: "serve -s build -l 8080"